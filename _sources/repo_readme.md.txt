# Distant Listening Corpus

Includes [dcml_corpora](https://github.com/DCMLab/dcml_corpora) (public) plus those that are to be published and merged into it. 

It has the `version_release` workflow installed. In order to automatically create a new version release that includes the whole corpus as datapackage, create and merge a Pull Request.